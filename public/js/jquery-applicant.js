$(function () {
    //Datemask dd/mm/yyyy
    $('.datemask').inputmask('yyyy-mm-dd', { 'placeholder': 'yyyy-mm-dd' })

    //Datemask dd/mm/yyyy
    $('.datemask-year').inputmask('yyyy', { 'placeholder': 'yyyy' })
});